diff --git a/src/chimera/chimera.h b/src/chimera/chimera.h
index 70b4523..247aa3d 100644
--- a/src/chimera/chimera.h
+++ b/src/chimera/chimera.h
@@ -341,7 +341,10 @@ struct WhisperOptions {
     bool detect_language = false;
 };
 struct whisper_context;
+// command_whisper is CLI-only; run_whisper stays for chimera.hpp's OOP wrapper.
+#ifndef CHIMERA_NO_CLI
 int command_whisper(const WhisperOptions & opts);
+#endif
 // Post-load body of command_whisper: takes a pre-loaded whisper_context
 // and runs the WAV-load -> resample -> transcribe -> diarize -> format-file
 // pipeline against it. Shared by command_whisper (loads then calls this)
@@ -512,7 +515,10 @@ struct SdOptions {
     std::string scm_policy;
 };
 struct sd_ctx_t;
+// command_sd is CLI-only; run_sd stays for chimera.hpp's OOP wrapper.
+#ifndef CHIMERA_NO_CLI
 int command_sd(const SdOptions & opts);
+#endif
 // Post-load body of command_sd: takes a pre-loaded sd_ctx_t and runs the
 // prompt/img2img/inpaint generation + PNG-write pipeline. Shared by
 // command_sd (loads then calls this) and chimera::SD::run() (uses its
@@ -728,9 +734,12 @@ int command_serve(const ServeOptions & opts);
 // for argv handling, while the optional OOP layer in chimera.hpp wraps them
 // for C++ consumers. `command_chat` stays in the CLI shell because it owns
 // the interactive REPL (color streaming, signal handling, linenoise).
+// CLI-only: called only from src/chimera_cli/chimera.cpp.
+#ifndef CHIMERA_NO_CLI
 int command_prompt(const LlamaCommonOptions & opts, const std::string & prompt);
 int command_embed(const EmbedOptions & opts);
 int command_tokenize(const TokenizeOptions & opts);
+#endif

 #ifdef CHIMERA_HAS_WHISPER
 void chimera_silence_whisper_log();
diff --git a/src/chimera/chimera_llama.cpp b/src/chimera/chimera_llama.cpp
index 3f28f62..0e5beac 100644
--- a/src/chimera/chimera_llama.cpp
+++ b/src/chimera/chimera_llama.cpp
@@ -935,6 +935,10 @@ std::string run_generation(
 }

 // ---- command entrypoints --------------------------------------------------
+// CLI-only: called solely from src/chimera_cli/chimera.cpp. Guarded out of
+// CHIMERA_NO_CLI builds; run_generation/run_generation_mtmd above (the
+// loader-side paths chimera.hpp's OOP wrappers use) are unaffected.
+#ifndef CHIMERA_NO_CLI

 int command_prompt(const LlamaCommonOptions & opts, const std::string & prompt) {
     if ((!opts.images.empty() || !opts.videos.empty()) && opts.mmproj.empty()) {
@@ -1121,3 +1125,5 @@ int command_embed(const EmbedOptions & opts) {
     }
     return 0;
 }
+
+#endif // CHIMERA_NO_CLI
diff --git a/src/chimera/chimera_sd.cpp b/src/chimera/chimera_sd.cpp
index d997680..225b758 100644
--- a/src/chimera/chimera_sd.cpp
+++ b/src/chimera/chimera_sd.cpp
@@ -1076,6 +1076,8 @@ void clear_log_buffer() {
 }  // namespace chimera_sd

 // ---- CLI subcommand ----------------------------------------------------
+// CLI-only: called solely from src/chimera_cli/chimera.cpp.
+#ifndef CHIMERA_NO_CLI

 // CLI driver: builds LoadParams from opts, loads sd_ctx_t, delegates the
 // rest to run_sd(ctx, opts). The OOP wrapper (chimera::SD) skips this and
@@ -1150,6 +1152,8 @@ int command_sd(const SdOptions & opts) {
     return run_sd(ctx.get(), opts);
 }

+#endif // CHIMERA_NO_CLI
+
 int run_sd(sd_ctx_t * ctx, const SdOptions & opts) {
     if (opts.prompt.empty()) {
         fail(ExitCode::BadInput, "sd requires --prompt");
diff --git a/src/chimera/chimera_whisper.cpp b/src/chimera/chimera_whisper.cpp
index dd6c1e2..398a1ce 100644
--- a/src/chimera/chimera_whisper.cpp
+++ b/src/chimera/chimera_whisper.cpp
@@ -915,6 +915,8 @@ void emit_format_file(const std::string & path,
 }  // namespace

 // ---- CLI subcommand ----------------------------------------------------
+// CLI-only: called solely from src/chimera_cli/chimera.cpp.
+#ifndef CHIMERA_NO_CLI

 // CLI driver: loads a fresh whisper_context from opts.model and delegates
 // to run_whisper(ctx, opts) for the actual pipeline. The OOP wrapper
@@ -936,6 +938,8 @@ int command_whisper(const WhisperOptions & opts) {
     return run_whisper(ctx.get(), opts);
 }

+#endif // CHIMERA_NO_CLI
+
 int run_whisper(whisper_context * ctx, const WhisperOptions & opts) {
     if (opts.input.empty()) {
         fail(ExitCode::BadInput, "whisper requires --input");
