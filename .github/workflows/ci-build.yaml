# PR verification build, debug-signed artifact. Pushes to main are built,
# signed and released by sync-and-release.yaml instead.
name: Build Android APK

permissions:
  contents: read

on:
  workflow_dispatch:
  pull_request:
    branches: ["main"]
    paths:
      - 'Android/**'
      - '.github/workflows/ci-build.yaml'
      - '.github/actions/**'

jobs:
  build_apk:
    name: Build Android APK (with submodules)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./Android/src
    steps:
      - name: Checkout the source code (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      # Analytics.kt shims upstream's Firebase Analytics API so incoming calls compile as
      # no-ops; the real SDK must never come back through either door.
      - name: Guard against Firebase Analytics returning
        run: |
          set -euo pipefail
          if grep -rln 'com\.google\.firebase\.analytics' app/src/main --include='*.kt'; then
            echo "Firebase Analytics import found above -- route through the Analytics.kt shim instead." >&2
            exit 1
          fi
          if grep -n 'firebase-analytics' gradle/libs.versions.toml app/build.gradle.kts; then
            echo "firebase-analytics dependency found above -- must stay removed." >&2
            exit 1
          fi
          echo "clean"

      - name: Set up build environment
        uses: ./.github/actions/android-build-env
        with:
          cache-prefix: ccache

      # abiFilters in the module build files already restrict this to arm64-v8a.
      - name: Build
        run: ./gradlew assembleRelease --no-daemon -PuseCcache

      # JVM-only task; -PuseCcache only affects native compilation.
      - name: Unit tests
        run: ./gradlew testDebugUnitTest --no-daemon

      - name: Check APK contents
        run: |
          set -euo pipefail
          APK=$(ls app/build/outputs/apk/release/*.apk)
          if ! unzip -l "$APK" | grep -q 'META-INF/services/kotlinx.coroutines.internal.MainDispatcherFactory'; then
            echo "APK has no MainDispatcherFactory service file (R8 stripped it); the app crashes at launch"
            exit 1
          fi
          echo "lib/arm64-v8a entries: $(unzip -l "$APK" | grep -c 'lib/arm64-v8a/')"

      - name: Show ccache stats
        run: ccache -s

      # keystore.properties is gitignored and absent in CI, so the build falls
      # back to debug signing on its own; no secrets needed here.
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: gallery-fork-release-apk
          path: Android/src/app/build/outputs/apk/release/*.apk
          if-no-files-found: error
